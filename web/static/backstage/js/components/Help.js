import React from 'react';

export default new React.createClass({

  render() {
      return (
        <div style={{textAlign:"center"}}>
          <h4>This is another page. For support, submit an issue on the Github page :)</h4>
        </div>
    );
  }
});
